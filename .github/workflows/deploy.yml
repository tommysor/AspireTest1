name: deploy

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
    outputs:
      webfrontendUrl:
        value: ${{ jobs.deploy.outputs.webfrontendUrl }}

env:
  GITHUB_SHA_SHORT: $(git rev-parse --short ${{ github.sha }})
  DEPLOY_TIMESTAMP: $(date +%Y-%m-%dT%H.%M.%S)
    
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    outputs:
      webfrontendUrl: ${{ steps.deploy-app-environment.outputs.webfrontendLatestRevisionFqdn }}

    env:
      RG_ACR: ${{ vars.RG_ACR }}
      RG_APP: ${{ vars.RG_APP }}
      APISERVICE_CONTAINERREPOSITORY: ${{ vars.APISERVICE_CONTAINERREPOSITORY }}
      WEBFRONTEND_CONTAINERREPOSITORY: ${{ vars.WEBFRONTEND_CONTAINERREPOSITORY }}
      CONTAINER_VERSION_TAG: ${{ github.run_id }}-${{ github.run_attempt }}
      ASPNETCORE_ENVIRONMENT: ${{ inputs.environment }}

    steps:
    - uses: actions/checkout@v4

    - name: 'Az login'
      uses: azure/login@v1
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

    - name: deploy acr
      id: deployAcr
      env:
        deploymentName: ${{ env.RG_ACR }}-${{ env.DEPLOY_TIMESTAMP }}-${{ env.GITHUB_SHA_SHORT }}
      run: |
        echo "Deploying Container registry"
        echo "deploymentName: ${{ env.deploymentName }}"

        az group create --location norwayeast --name ${{ env.RG_ACR }}

        az bicep lint --file .platform/main.acr.bicep

        deployResult=$(az deployment group create \
          --mode Complete \
          --resource-group "${{ env.RG_ACR }}" \
          --name "${{ env.deploymentName }}" \
          --template-file ./.platform/main.acr.bicep \
          )

        echo "# Deploy result"
        echo $deployResult
        echo "# ---"
        
        echo "# setting outputs"
        
        # containerRegistryUrl
        containerRegistryUrl=$(echo $deployResult | jq --raw-output '.properties.outputs.containerRegistryUrl.value')
        echo "containerRegistryUrl=$containerRegistryUrl" >> $GITHUB_OUTPUT
        # managedIdentityId
        managedIdentityId=$(echo $deployResult | jq --raw-output '.properties.outputs.managedIdentityId.value')
        echo "managedIdentityId=$managedIdentityId" >> $GITHUB_OUTPUT
        # managedIdentityClientId
        managedIdentityClientId=$(echo $deployResult | jq --raw-output '.properties.outputs.managedIdentityClientId.value')
        echo "managedIdentityClientId=$managedIdentityClientId" >> $GITHUB_OUTPUT
        
        echo "# done"

    - name: login containerRegistry
      run: az acr login --name ${{ steps.deployAcr.outputs.containerRegistryUrl }}

    - name: publish apiservice
      run: |
        echo "Publishing apiservice"

        pushd src/Test1.ApiService

        dotnet publish \
          -p ContainerRegistry='${{ steps.deployAcr.outputs.containerRegistryUrl }}' \
          -p PublishProfile=DefaultContainer \
          -p ContainerImageTags='"${{ env.CONTAINER_VERSION_TAG }}"' \
          -p ContainerRepository=${{ env.APISERVICE_CONTAINERREPOSITORY }}

        popd

    - name: publish webfrontend
      run: |
        echo "Publishing webfrontend"

        pushd src/Test1.Web

        dotnet publish \
          -p ContainerRegistry='${{ steps.deployAcr.outputs.containerRegistryUrl }}' \
          -p PublishProfile=DefaultContainer \
          -p ContainerImageTags='"${{ env.CONTAINER_VERSION_TAG }}"' \
          -p ContainerRepository=${{ env.WEBFRONTEND_CONTAINERREPOSITORY }}

        popd

    - name: deploy app environment
      id: deploy-app-environment
      env:
        deploymentName: ${{ env.RG_APP }}-${{ env.DEPLOY_TIMESTAMP }}-${{ env.GITHUB_SHA_SHORT }}
        APISERVICE_CONTAINER_IMAGE: ${{ steps.deployAcr.outputs.containerRegistryUrl }}/${{ env.APISERVICE_CONTAINERREPOSITORY }}:${{ env.CONTAINER_VERSION_TAG }}
        WEBFRONTEND_CONTAINER_IMAGE: ${{ steps.deployAcr.outputs.containerRegistryUrl }}/${{ env.WEBFRONTEND_CONTAINERREPOSITORY }}:${{ env.CONTAINER_VERSION_TAG }}
      run: |
        echo "Deploying applications"

        az group create --location norwayeast --name ${{ env.RG_APP }}

        az bicep lint --file .platform/main.app.bicep

        deployResult=$(az deployment group create \
          --mode Incremental \
          --resource-group "${{ env.RG_APP }}" \
          --name "${{ env.deploymentName }}" \
          --template-file ./.platform/main.app.bicep \
          --parameters aspnetcoreEnvironment=${{ env.ASPNETCORE_ENVIRONMENT }} \
          --parameters containerRegistryUrl=${{ steps.deployAcr.outputs.containerRegistryUrl }} \
          --parameters managedIdentityClientId=${{ steps.deployAcr.outputs.managedIdentityClientId }} \
          --parameters managedIdentityId=${{ steps.deployAcr.outputs.managedIdentityId }} \
          --parameters apiserviceContainerImage=${{ env.APISERVICE_CONTAINER_IMAGE }} \
          --parameters webfrontendContainerImage=${{ env.WEBFRONTEND_CONTAINER_IMAGE }} \
          --parameters deployTimestamp=${{ env.DEPLOY_TIMESTAMP }} \
          )
        
        echo "# Deploy result"
        echo $deployResult
        echo "# ---"

        echo "# setting outputs"
        
        # webfrontendLatestRevisionFqdn
        webfrontendLatestRevisionFqdn=$(echo $deployResult | jq --raw-output '.properties.outputs.webfrontendLatestRevisionFqdn.value')
        echo "webfrontendLatestRevisionFqdn=$webfrontendLatestRevisionFqdn" >> $GITHUB_OUTPUT
        echo "webfrontendLatestRevisionFqdn: $webfrontendLatestRevisionFqdn"
